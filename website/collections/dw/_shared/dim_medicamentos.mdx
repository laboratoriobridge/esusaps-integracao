| Coluna DW                         | Dimensão referência |
| --------------------------------- | ------------------- |
| co\_dim\_catmat                   | Código CATMAT. Campo `co_seq_dim_catmat` da `tb_dim_catmat` |
| co\_dim\_dose\_frequencia\_medida | Código da periodicidade da dose. Campo `co_seq_dim_dose_frequencia_medida` da `tb_dim_dose_frequencia_medida` |
| co\_dim\_duracao\_tratamento\_med | Código da duração do tratamento. Campo `co_seq_dim_duracao_tratamento_med `da `tb_dim_duracao_tratamento_med` |
| co\_dim\_dose\_frequencia         | Código do tipo de periodicidade da dose. Campo `co_seq_dim_dose_frequencia` da `tb_dim_dose_frequencia`  |
| co\_dim\_via\_administracao       | Código da via de administração da dose. Campo `co_seq_dim_via_administracao` da `tb_dim_via_administracao` |