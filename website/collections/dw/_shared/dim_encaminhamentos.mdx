| Coluna DW                              | Dimensão referência |
| -------------------------------------- | ------------------- |
| co\_dim\_especialidade                 | Código da especialidade do profissional de saúde para qual o cidadão foi encaminhado. Campo `co_seq_dim_especialidade` da `tb_dim_especialidade` |
| co\_dim\_classificacao\_risc\_enc      | Código da classificação de risco do encaminhamento. Campo `co_seq_dim_classific_risc_enc` da `tb_dim_classificacao_risc_enc` |
| co\_dim\_classificacao\_prioridad      | Código da classificação de prioridade do encaminhamento. Campo `co_seq_dim_prioridade_cuidado` da `tb_dim_prioridade_cuidado` |
| co\_dim\_ciap2                         | Código do CIAP2 utilizado no encaminhamento. Campo `co_seq_dim_ciap2` da `tb_dim_ciap2` |
| co\_dim\_cid10                         | Código do CID10 utilizado no encaminhamento. Campo `co_seq_dim_cid10` da `tb_dim_cid10` |
| co\_dim\_cbo\_guia\_encaminhamento     | Código do CBO do profissional que assina o guia de encaminhamento. Campo `co_seq_dim_cbo` da `tb_dim_cbo` |