| Coluna DW | Dimensão referência | 
| --------- | ------------------- | 
| co\_dim\_tempo | Responsável por armazenar as datas de maneira estruturada. Campo `co_seq_dim_tempo` da `tb_dim_tempo` | 
| co\_dim\_cid | Código da CID do problema ou condição. Campo `co_seq_dim_cid` da  `tb_dim_cid` | 
| co\_dim\_ciap | Código da CIAP do problema ou condição. Campo `co_seq_dim_ciap` da `tb_dim_ciap` | 
| co\_dim\_situacao | Código da situação do problema ou condição. Campo `co_seq_dim_situacao` da  `tb_dim_situacao_problema` | 
| co\_dim\_data\_inicio\_problema | Responsável por armazenar as datas de maneira estruturada. Campo `co_seq_dim_tempo` da `tb_dim_tempo` | 
| co\_dim\_data\_fim\_problema | Responsável por armazenar as datas de maneira estruturada. Campo `co_seq_dim_tempo` da `tb_dim_tempo` | 